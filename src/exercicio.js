"use strict";
function CalculaIMC(peso, altura) {
    return peso * altura;
}
function BemVindo(nome, saudacao) {
    nome = "denis",
        saudacao = "Bem Vindo";
    console.log(saudacao + `${nome}`);
}
